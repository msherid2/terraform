builds:
  - goos:
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=0
archives:
  - format: binary
checksum:
  name_template: checksums.txt
  algorithm: sha256
changelog:
  skip: true
